# How to run locally

`docker compose up --build`

## Running the Database Only

To run only the MongoDB database using Docker Compose, use the following command:

`docker compose -f docker-compose.db-only.yml up --build`
